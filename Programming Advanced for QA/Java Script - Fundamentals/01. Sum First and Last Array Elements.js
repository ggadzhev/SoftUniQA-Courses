function solveArrays(input){
    let firstArrayElement = input[0];
    let lastArrayElement = input[input.length - 1];
    console.log(firstArrayElement + lastArrayElement);
}
solveArrays([20, 30, 40]);
